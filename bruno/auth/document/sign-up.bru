meta {
  name: sign-up
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/{{path}}/sign-up?db=document
  body: json
  auth: inherit
}

params:query {
  db: document
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "doro-hd",
    "password": "adminAdmin",
    "confirm-password": "adminAdmin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
