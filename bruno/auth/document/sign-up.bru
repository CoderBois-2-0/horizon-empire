meta {
  name: sign-up
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/{{path}}/sign-up?db={{db}}
  body: json
  auth: inherit
}

params:query {
  db: {{db}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "doro-hd",
    "password": "adminAdmin",
    "confirm-password": "adminAdmin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
