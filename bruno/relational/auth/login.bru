meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/{{path}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "doro-hd",
    "password": "adminAdmin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
