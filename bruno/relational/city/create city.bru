meta {
  name: create city
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/{{path}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Suna",
    "mapType": "desert",
    "mapSize": "large"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
