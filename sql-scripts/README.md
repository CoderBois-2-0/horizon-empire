# SQL Scripts
The files in here contain sql code that could not be achivede using drizzle's typesafe API. Be sure to apply the scripts in here after having applied the scripts in the drizzle folder.

Be sure to fill out placeholder data like password fields.
The following files need to be modified before they should be applied to the database:
- 0002._create_read_only_role.sql
- 0003._create_read_write_role.sql
- 0004._create_app_users.sql
